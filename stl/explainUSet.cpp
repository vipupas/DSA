#include<bits/stdc++.h>
using namespace std;

void explainUSet(){
	unordered set<int> st;
	// NO SORTED orded
	// it is UNIQUE
	// lower_bound and upper_bound function does not work
	// rest are same 
	// does not store in any order
	// complexity is better than set , except when collision happens
	// complexity -> O(1) and worst O(N)
}
